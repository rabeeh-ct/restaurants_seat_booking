import 'package:flutter/material.dart';

class FoodSelectingScreen extends StatelessWidget {
  const FoodSelectingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
